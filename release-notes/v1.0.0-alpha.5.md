Maintenance pre-release. Nothing Consumer-facing changed: the reusable workflows, runtime files, skills, and Caller inputs are the same as in `v1.0.0-alpha.4`. Upgrading is optional; if you do, move your `uses:` pins and copied files together.

### Changed

- The repository's `CODEOWNERS` contract test now requires root-anchored rules, names the missing anchor in its failure message, and gives the same result for wildcard patterns on every supported Python version.

Pin reusable workflows with `uses: <owner>/agent-kit/.github/workflows/<file>@v1.0.0-alpha.5` and copy skills plus `.github/agent-runtime/`, `.github/security-audit/`, and `.github/agent-pipeline/` from the same tag. See `docs/adopt.md`.
